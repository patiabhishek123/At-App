name: student_app
description: "AtApp Student Mobile Client for real-time attendance check-ins and metrics tracker."
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  intl: ^0.19.0
  fl_chart: ^0.66.0
  geolocator: ^10.1.0
  network_info_plus: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
