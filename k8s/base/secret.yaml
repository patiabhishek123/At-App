# This is a TEMPLATE, not a real secret: every value below is a placeholder.
# Do not commit real credentials here. In a real cluster, populate this via
# your secrets tooling instead of applying this file directly, e.g.:
#   - Kubernetes Secrets created out-of-band (kubectl create secret / CI)
#   - Sealed Secrets (bitnami-labs/sealed-secrets) committed in their
#     encrypted form
#   - External Secrets Operator syncing from AWS Secrets Manager / Vault /
#     GCP Secret Manager
apiVersion: v1
kind: Secret
metadata:
  name: atapp-server-secrets
type: Opaque
stringData:
  DB_USER: "CHANGEME"
  DB_PASSWORD: "CHANGEME"
  JWT_SECRET: "CHANGEME"
  PLATFORM_ADMIN_KEY: "CHANGEME"
  SMTP_USERNAME: "CHANGEME"
  SMTP_PASSWORD: "CHANGEME"
  FCM_SERVICE_ACCOUNT_JSON: ""
